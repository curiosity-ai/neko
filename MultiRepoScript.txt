using System;
using System.IO;
using System.Collections.Generic;

// testing logic
var input = ".";
var multiRepo = true;
if (multiRepo) {
    var directories = Directory.GetDirectories(input);
    foreach (var dir in directories) {
        if (File.Exists(Path.Combine(dir, "neko.yml"))) {
            Console.WriteLine(dir);
        }
    }
}
